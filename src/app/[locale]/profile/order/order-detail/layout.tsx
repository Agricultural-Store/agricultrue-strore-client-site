import React, { ReactNode } from "react";

type Props = {
  children: ReactNode;
};

const ProfileOrderDetailLayout = ({ children }: Props) => {
  return <>{children}</>;
};

export default ProfileOrderDetailLayout;
