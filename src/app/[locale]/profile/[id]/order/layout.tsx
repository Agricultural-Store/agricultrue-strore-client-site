import React, { ReactNode } from "react";

type Props = {
  children: ReactNode;
};

const ProfileOrderLayout = ({ children }: Props) => {
  return <>{children}</>;
};

export default ProfileOrderLayout;
