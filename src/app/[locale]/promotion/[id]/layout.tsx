import React, { ReactNode } from "react";

type Props = {
  children: ReactNode;
};

const PromotionDetailLayout = ({ children }: Props) => {
  return <>{children}</>;
};

export default PromotionDetailLayout;
